import 'package:flutter/material.dart';

class GameFormatsPage extends StatelessWidget {
  const GameFormatsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Game Formats Page"),
      ),
    );
  }
}
